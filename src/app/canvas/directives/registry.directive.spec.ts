import { RegistryDirective } from './registry.directive';

describe('RegistryDirective', () => {
  it('should create an instance', () => {
    const directive = new RegistryDirective();
    expect(directive).toBeTruthy();
  });
});
