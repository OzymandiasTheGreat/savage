import { AttrsDirective } from './attrs.directive';

describe('AttrsDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrsDirective();
    expect(directive).toBeTruthy();
  });
});
